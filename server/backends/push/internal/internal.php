<?php

    /**
     * backends push namespace
     */

    namespace backends\push {

        use backends\backend;

        /**
         * internal push class
         */

        class internal extends push {

            /**
             * @param $tokenType
             * @param $token
             * @param $payload
             * @return boolean
             */
            public function push($tokenType, $token, $payload)
            {
                // TODO: Implement push() method.
            }
        }
    }

