# Backend: CDR (`server/backends/cdr/`)

В этой папке находится backend-модуль CDR (детализация звонков / call detail records).

## Содержание

- [`cdr.php`](./cdr.ru.md) — базовый abstract-класс (`backends\cdr\cdr`)

