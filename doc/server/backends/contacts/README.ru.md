# Backend `contacts`

## Назначение

Контакты (расширяемая точка интеграции). Базовый класс пустой; поведение задаёт variant.

## Код

- **Базовый класс**: `server/backends/contacts/contacts.php`.
- **Варианты**: `internal`.

## Конфигурация

Ключ в `server/config/config.json`: `backends.contacts`.

## Основные методы (контракт)

См. реализацию в `internal/internal.php`.

## Кто использует

По проекту — связанные API/модули.

См. также [общий индекс backend’ов](../README.ru.md) и [загрузчик `loader.php`](../../utils/loader.ru.md).

