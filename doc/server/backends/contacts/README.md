# `contacts` backend

## Purpose

Contacts subsystem; empty abstract base — behavior lives in the variant.

## Code

- **Base class**: `server/backends/contacts/contacts.php`.
- **Variants**: `internal`.

## Configuration

Key in `server/config/config.json`: `backends.contacts`.

## Main API (contract)

See `internal/internal.php`.

## Callers

Project-specific APIs/modules.

See also the [backends index](../README.md) and [`loader.php`](../../utils/loader.md).

