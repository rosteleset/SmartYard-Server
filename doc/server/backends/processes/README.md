# `processes` backend

## Purpose

Placeholder backend; empty abstract base — extend per deployment needs.

## Code

- **Base class**: `server/backends/processes/processes.php`.
- **Variants**: `internal`.

## Configuration

Key in `server/config/config.json`: `backends.processes`.

## Main API (contract)

—

## Callers

As wired in config.

See also the [backends index](../README.md) and [`loader.php`](../../utils/loader.md).

